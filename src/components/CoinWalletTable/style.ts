import {makeStyles} from "@material-ui/core/styles";

export const useStyles = makeStyles({
    table: {
        minWidth: 650,
    },
    buttonWrap: {
        paddingBottom: '40px',
        paddingLeft: '20px'
    },
    button: {
        top: '10px',
        padding: '0 25px',
        textAlign: 'center',
    }
});